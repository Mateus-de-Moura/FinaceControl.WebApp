trigger:
- main  

pool:
  name: default 

variables:
  nodeVersion: '18.x'
  buildDir: 'dist'  
  destinationPath: 'C:\Users\Mateus\Desktop\PublishWeb' 
  # viteApiUrl: 'http://192.168.18.198:8080/'  
  viteApiUrl: 'https://localhost:7113/'

steps:
- task: NodeTool@0
  displayName: 'Instalar Node.js'
  inputs:
    versionSpec: '$(nodeVersion)'
    checkLatest: true

- script: |
    npm install
  displayName: 'Instalar dependências'

- powershell: |
    $envContent = "VITE_API_URL=$(viteApiUrl)"
    Set-Content -Path "$(System.DefaultWorkingDirectory)\.env.production" -Value $envContent -Encoding UTF8
  displayName: 'Criar .env.production com URL da API'

- script: |
    npm run build
  displayName: 'Executar build do React (Vite)'

- powershell: |
    $webConfigPath = "$(System.DefaultWorkingDirectory)\$(buildDir)\web.config"
    $webConfigLines = @(
      '<?xml version="1.0"?>'
      '<configuration>'
      '  <system.webServer>'
      '    <rewrite>'
      '      <rules>'
      '        <rule name="React Routes" stopProcessing="true">'
      '          <match url=".*" />'
      '          <conditions logicalGrouping="MatchAll">'
      '            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />'
      '            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />'
      '            <add input="{REQUEST_URI}" pattern="^/(api)" negate="true" />'
      '          </conditions>'
      '          <action type="Rewrite" url="/index.html" />'
      '        </rule>'
      '      </rules>'
      '    </rewrite>'
      '  </system.webServer>'
      '</configuration>'
    )
    $webConfigContent = $webConfigLines -join "`r`n"
    Set-Content -Path $webConfigPath -Value $webConfigContent -Encoding UTF8
  displayName: 'Criar web.config corrigido'

- powershell: |
    $source = "$(System.DefaultWorkingDirectory)\$(buildDir)"
    $destination = "$(destinationPath)"

    if (!(Test-Path -Path $destination)) {
      New-Item -ItemType Directory -Path $destination | Out-Null
    }

    Copy-Item -Path "$source\*" -Destination $destination -Recurse -Force

    Write-Host "Build do React copiado com sucesso para: $destination"
  displayName: 'Copiar build para IIS'
